"""multi-agent-bootstrap CLI package (P-0059 Phase 3).

Wraps cookiecutter + governance-core install + bootstrap scripts so users get
a working multi-agent Claude Code project with one command.

    uvx --from . multi-agent-bootstrap new my-project
    # or
    pip install -e .
    multi-agent-bootstrap new my-project
"""

__version__ = "0.2.1"

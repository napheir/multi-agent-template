# .governance/

Populated by `governance-core install`:

- `config.json` (project config)
- `clauses/art_*.md` (rendered generic clauses)
- `agents.json`, `core_keywords.json`, `sync_files.json` (config injection)
